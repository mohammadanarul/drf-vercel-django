{
    "builds": [{
        "src": "core/wsgi.py",
        "use": "@ardnt/vercel-core-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "core/wsgi.py"
        }
    ]
}